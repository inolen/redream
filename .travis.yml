language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
    apt:
      packages:
        - cmake
      sources:
        - kubuntu-backports

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
